1. 每次写入都会清除缓存区:  `_, err = scanner.WriteString(data) `
2. 使用Flush把缓存区内容写入文件:  `scanner.Flush() `
3. testcode:  `package CodeSummary `
4. 列表添加元素: 
```js
files = append(files, path) 
return nil                  
```
5. 遍历文件夹: 
```js
filepath.Walk(folderPath, func(path string, info fs.FileInfo, err error) error { 
files = append(files, path) //SUM 列表添加元素
return nil                  
}) 
```
6. 把文件放入扫描器，可以一行一行地读取:  `scanner := bufio.NewScanner(file) `
7. 找到不同的组:  `result := exp.FindStringSubmatch(text) `
8. 清空切片 重新分配一个切片:  `matchedTexts = []string{} `

